/system identity
set name="{{ hostname }}"

{% for iface in interfaces %}
/interface ethernet
set {{ iface.name }} disabled=no

/ip address
add address={{ iface.ip }}/{{ iface.mask }} interface={{ iface.name }}
{% endfor %}
